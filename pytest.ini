[pytest]
# Every lab across all modules names its tests test_correctness.py (MODULE_FORMAT.md).
# importlib mode lets pytest import same-named files from different directories.
addopts = --import-mode=importlib
